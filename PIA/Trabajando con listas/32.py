# -*- coding: utf-8 -*-

# Usa enumerate() para imprimir el índice y valor de cada elemento de una lista.

lista = ['a', 'b', 'c', 'd', 'e']
for indice, valor in enumerate(lista):
    print(f'Índice: {indice}, Valor: {valor}')
